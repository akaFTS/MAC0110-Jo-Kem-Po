function calculateError (originalImg, decompressedImg)    % lemos as imagens    oimg = imread(originalImg);    dimg = imread(decompressedImg);        % fazemos uma conversão para double    oimg = double(oimg);    dimg = double(dimg);    oimg = cat(3, oimg, oimg, oimg);    dimg = cat(3, dimg, dimg, dimg);    % calculamos o erro relativo com a função norm()    errR = norm(oimg(:,:,1) - dimg(:,:,1)) / norm(oimg(:,:,1));    errG = norm(oimg(:,:,2) - dimg(:,:,2)) / norm(oimg(:,:,2));    errB = norm(oimg(:,:,3) - dimg(:,:,3)) / norm(oimg(:,:,3));        % calculamos a média dos erros e exibimos    err = (errR + errG + errB) / 3;    disp(err);